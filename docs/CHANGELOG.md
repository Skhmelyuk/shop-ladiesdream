# Changelog

Всі значущі зміни в проекті **ShopDj** фіксуються тут.

---

## [2026-05-01]

### CSS / UI
- Повна реалізація табличного дизайну кошика (Варіант 2) з мінімалістичним стилем
- Створено HTML структуру таблиці з 4 колонками: Фото, Товар, Кількість, Сума
- Додано CSS стилі для табличного дизайну з компактним відображенням
- Реалізовано адаптивність: таблиця на desktop, карточки на mobile (<768px)
- Оновлено JavaScript для підтримки нових класів табличного дизайну
- Збережено всю функціональність валідації кількості та обмежень stock
- **Новий дизайн карточок товарів в каталозі** з сучасним виглядом та ефектами
- **Замінено кнопку "Додати в кошик" на "Переглянути товар"** в каталозі

### Деталі табличного дизайну:
- Компактні рядки висотою ~70px з hover ефектами
- Зображення товарів 50x50px з мінімальними border'ами
- Чітка типографіка: назва 14px, варіанти 12px, ціни 14px
- Центровані елементи керування кількістю
- Мінімалістична кольорова палітра з нейтральними тонами
- Оптимізована щільність інформації для швидкого сканування

### Новий дизайн карточок товарів:
- Сучасні карточки з rounded corners (16px) та м'якими тінями
- Зображення товарів 240px висота зі статичним відображенням
- Перероблені бейджі: градієнтні для знижок, круглі для рекомендованих
- Покращена типографіка та ієрархія інформації
- Кнопка "Переглянути товар" з градієнтним фоном та іконкою
- Адаптивна grid-сітка: 3 колонки (desktop), 2 (tablet), 1 (mobile)

### Інтерактивність фото товарів:
- **Статичні фото при наведенні** (вимкнено масштабування для чистого вигляду)
- **Модальне вікно для детального перегляду** при кліку на фото
- Курсор zoom-in для вказівки можливості клікнути на фото
- Інформація про товар в модальному вікні (назва та опис)
- Закриття модального вікна: клік на фон, ESC, або кнопку ×
- Повна адаптивність модального вікна для всіх пристроїв

### Модальне вікно на сторінці деталей товару:
- **Клікабельне головне фото** з курсором zoom-in для відкриття модального вікна
- **Фільтрація фото за кольором** - показуються тільки фото вибраного кольору
- **Динамічне оновлення** при зміні кольору товару
- **Стрілки навігації** (←→) для переключення фото в межах кольору
- **Мініатюри фото** під головним зображенням з активним станом
- **Керування клавіатурою**: стрілки для навігації, ESC для закриття
- **Інформація про товар**: назва та ціна під фото
- **Автоматична синхронізація** з поточним фото на сторінці
- **Плавні анімації** та переходи для всіх елементів
- **Адаптивний дизайн** для всіх розмірів екрану

### Виправлення логіки фото за кольорами:
- **Прибрано головне фото** з масиву фото для кожного кольору
- **Тільки релевантні фото** - показуються фото відповідного кольору
- **Fallback логіка** - якщо для кольору немає фото, використовується головне фото
- **Коректна фільтрація** - кожен колір показує тільки свої фотографії

### Покращена секція відгуків:
- **Сучасний дизайн карточок відгуків** з hover ефектами та тіннями
- **Аватори користувачів** з градієнтним фоном та іконками
- **Дата відгуку** під іменем автора для кращої орієнтації
- **Покращене відображення рейтингу** з числовим значенням
- **Структуровані переваги та недоліки** з кольоровим кодуванням
- **Умовне відображення коментарів** - показуються тільки якщо є текст
- **Покращений стан без відгуків** з іконкою та закликом до дії
- **Адаптивний дизайн** для всіх розмірів екрану
- **Інформативні підказки** для неавторизованих користувачів

### Виправлення відображення оцінки товару:
- **Числовий рейтинг** завжди відображається біля зірок
- **Підпис "Ще немає оцінки"** для товарів без відгуків
- **Правильне відображення** при rating = 0
- **Покращена граматика** для кількості відгуків (відгук/відгуки/відгуків)
- **Hover ефекти** для посилання на відгуки
- **Завжди видимий блок рейтингу** незалежно від наявності оцінок

### Виправлення підсвічування зірок рейтингу:
- **Прибрано округлення** рейтингу для коректного порівняння
- **Використовується повне значення** average_rating без floatformat:"0"
- **Додано default:0** для обробки відсутності рейтингу
- **Покращена логіка порівняння** зірок з числовим значенням
- **Дебаг коментар** для перевірки значень (можна прибрати)
- **Коректне підсвічування** зірок відповідно до реальної оцінки

### Новий мінімалістичний дизайн відгуків:
- **Чистий мінімалістичний стиль** з тонкими лініями та багато пробілів
- **Компактний список відгуків** з малими відступами
- **Прогрес-бари рейтингу** замість зірок (60px ширина, рожевий колір)
- **Монохромні аватари** з ініціалами користувачів
- **Субтльні hover ефекти** та плавні переходи
- **Адаптивний дизайн** для мобільних пристроїв

### Підтримка фото/відео у відгуках:
- **Нові моделі** ReviewImage та ReviewVideo для медіа файлів
- **Мініатюри фото** під текстом відгуку (60x60px)
- **Іконки відео** з можливістю перегляду
- **Медіа модальне вікно** для перегляду фото/відео
- **Анімації** при наведенні на медіа елементи
- **Drag & drop** підготовка для завантаження

### Відповіді на відгуки:
- **Ієрархічна структура** з parent_review полем
- **Візуальні відступи** для відповідей (42px зліва)
- **Кнопка "Відповісти"** для авторизованих користувачів
- **Форма відповіді** в модальному вікні
- **Менший розмір** аватарів для відповідей (24px)
- **Сірий фон** для візуального відокремлення

### Технічні покращення:
- **Розширена модель Review** з parent_review та is_reply
- **Нові міграції** для ReviewImage та ReviewVideo
- **JavaScript функції** для медіа та відповідей
- **CSS стилі** для мінімалістичного дизайну
- **Анімація прогрес-барів** при завантаженні сторінки

### Виправлення помилки фільтра 'mul':
- **Додано завантаження** math_filters в шаблон product-detail.html
- **Фільтр `mul`** тепер доступний для множення рейтингу на 20%
- **Прогрес-бари рейтингу** коректно відображають ширину
- **TemplateSyntaxError** виправлено, сторінка працює

### Виправлення помилки 'main_tags' is not a registered tag library:
- **Видалено неіснуючий** main_tags з завантаження шаблону
- **Тег `make_list`** знаходиться в math_filters, тому main_tags не потрібен
- **TemplateSyntaxError** виправлено, всі теги доступні
- **Сторінка деталей товару** тепер працює без помилок

### Виправлення помилки 'get_item' is not a valid filter:
- **Замінено `get_item`** на `get` в шаблоні product-detail.html
- **Фільтр `get`** існує в dict_filters.py для доступу до словника
- **TemplateSyntaxError** виправлено, доступ до sizes_with_stock працює
- **Розміри товару** коректно відображаються з доступністю

### Виправлення повторної помилки фільтра 'mul' - розрахунок у view:
- **Додано `reviews_with_width`** у views.py з розрахованими значеннями rating_width
- **Замінено фільтр `mul`** на готове значення `review_item.rating_width`
- **Оновлено всі посилання** з `review` на `review_item.review` в шаблоні
- **TemplateSyntaxError** остаточно виправлено, прогрес-бари працюють
- **Надійне рішення** - не залежить від template фільтрів

### Виправлення помилки фільтра 'mul' у відповідях на відгуки:
- **Додано `replies_with_width`** у views.py з розрахованими значеннями для відповідей
- **Замінено цикл відповідей** на `review_item.replies_with_width`
- **Оновлено всі посилання** з `reply` на `reply_item.reply` в шаблоні
- **Прогрес-бари відповідей** тепер працюють без помилок
- **Повна сумісність** - всі rating_width розраховуються в view

### Створення AddReplyView та URL для відповідей на відгуки:
- **Додано `AddReplyView`** в reviews/views.py для обробки відповідей
- **Реалізовано POST-обробку** з валідацією та створенням відповідей
- **Додано URL `add-reply/`** в reviews/urls.py з назвою 'add_reply'
- **NoReverseMatch помилка** виправлено, URL 'reviews:add_reply' працює
- **JSON відповідь** для AJAX обробки форми відповідей
- **Відповіді одразу активні** та мають рейтинг 5 за замовчуванням

### Виправлення кнопки 'Відповісти' на відгуки - JavaScript функціонал:
- **Додано `showReplyForm()`** функцію для показу форми відповідей
- **Додано `closeReplyForm()`** функцію для закриття форми
- **Реалізовано AJAX-відправку** форми з обробкою помилок
- **Додано кнопку закриття** × у формі відповідей для кращого UX
- **Автоматичне перезавантаження** сторінки після успішної відповіді
- **Індикація завантаження** під час відправки форми
- **Кнопка 'Відповісти'** тепер повністю функціональна

### Створення модального вікна відповідей по центру екрану:
- **Перетворено форму відповідей** на повноцінне модальне вікно
- **Додано CSS стилі** `.reply-modal` з центруванням по екрану
- **Реалізовано анімацію** появи/зникнення з `replyModalSlideIn`
- **Додано напівпрозорий фон** з blur ефектом для фокусу
- **Центрування по вертикалі та горизонталі** з `align-items: center`
- **Закриття при кліку на фон** для кращого UX
- **Блокування скролу сторінки** під час відкритого модального вікна
- **Адаптивний дизайн** з максимальною шириною 500px

### Покращення дизайну модального вікна відповідей - сучасний стиль:
- **Градієнтний фон** з рожевими акцентами та blur ефектом
- **Преміум тіні** багаторівневі з рожевими відблисками
- **Райдужний header** з градієнтною смужкою зверху
- **Градієнтний текст** для заголовка з ефектом gold
- **Анімована кнопка закриття** з rotate ефектом при hover
- **3D ефекти** для textarea з inset тінями та transform
- **Шimmer ефекти** на кнопках з анімованими блисками
- **Пульсуюча аура** навколо модального вікна
- **Покращена анімація** з scale, blur та cubic-bezier
- **Неонові glow ефекти** при фокусі та hover станах

### Додаткові ефекти для модального вікна відповідей - shimmer та glow:
- **Декоративний shimmer border** з анімованим градієнтом по периметру
- **Ефект світіння** при фокусі на будь-якому елементі форми
- **Hover ефекти для textarea** з плавною зміною border-color
- **Backdrop фільтри** з blur та saturation для глибини
- **Анімація shimmer** з rotate(45deg) для діагонального блиску
- **Стилізація успішного стану** з зеленими акцентами
- **Покращена прозорість** та border з білими відблисками

### Стилізація кнопки "Відповісти" з градієнтами та анімаціями:
- **Градієнтний фон** з рожевих переходів
- **Shimmer ефект** при hover з анімованим блиском
- **Анімація іконки** зі зміщенням при hover
- **3D transform** з translateY(-2px) при наведенні
- **Розширення тіней** з box-shadow growth
- **Active стан** з return transform для тактильного відчуття
- **Круглі бордери** (20px border-radius) для сучасного вигляду

### Стилізація поля оцінки у формі відповідей:
- **Кастомний select** з appearance: none та рожевою стрілкою
- **Градієнтний фон** та inset тіні для глибини
- **Hover ефекти** з зміною border-color та transform
- **Focus стани** з неоновим glow ефектом
- **Зірочки в option** для візуального представлення оцінки
- **Плавні переходи** з cubic-bezier для всіх станів
- **Розширений padding** для кращого UX

### Покращення дизайну відгуків:
- **Зірки замість прогрес-бару** — `★★★★☆` з золотим кольором та тінями
- **Нові картки відгуків** з border-radius 16px та hover ефектом підйому
- **Рожева смужка зліва** з'являється при hover на картці
- **Градієнтний аватар** з тінню замість сірого кружка
- **Grid layout для переваг/недоліків** — 2 колонки поруч
- **Іконки thumbs-up/down** в кольорових кружках замість плюс/мінус
- **Зелені/червоні блоки** для переваг/недоліків з кольоровими рамками
- **Відповідь адміна** з рожевою смугою зліва та градієнтним фоном
- **Більші медіа-прев'ю** (72x72) з rounded кутами
- **Адаптивні стилі** для всіх розмірів екранів

### Нова модель ReviewReply для відповідей адміністратора:
- **Створено окрему модель `ReviewReply`** замість використання `Review` для відповідей
- **Поля**: review (FK), author, content, created_at, updated_at, is_active
- **Тільки адміністратори** (`is_staff`) можуть залишати відповіді
- **Відповіді не впливають** на рейтинг товару
- **Прибрано поле оцінки** з форми відповіді
- **Кнопка "Відповісти"** тепер видима лише адмінам (`{% if user.is_staff %}`)
- **Відображення admin-badge** з іконкою щита та міткою "Адміністратор"
- **Зареєстровано в адмін-панелі** через `ReviewReplyAdmin`
- **Міграція**: `reviews/migrations/0004_reviewreply.py`

### Виправлення помилки duplicate key при відповіді на відгук:
- **Причина**: `unique_together = ['product', 'author']` в моделі `Review` блокувала другий запис від того самого автора на той самий продукт
- **Рішення**: прибрано `unique_together` з `Meta` класу моделі
- **Міграція**: `reviews/migrations/0003_alter_review_unique_together.py`
- **Наслідок**: один користувач тепер може залишати відповіді до кількох різних відгуків на одному продукті

### Заміна alert на кастомне сповіщення з преміум дизайном:
- **Сучасний дизайн** з градієнтами та тінями
- **Анімована поява** з slideIn ефектом справа
- **Різні типи сповіщень** (success/error) з кольоровими акцентами
- **Автоматичне ховання** через 4 секунди
- **Кнопка закриття** з hover ефектами
- **Градієнтна смужка** зверху для візуального акценту
- **Іконки** FontAwesome для різних типів повідомлень
- **Responsive дизайн** з адаптивною шириною

### Покращена кнопка закриття модального вікна:
- **Сучасний круглий дизайн** з білим фоном та тінню
- **Інтуїтивне розташування** у правому верхньому куті
- **Hover ефекти**: масштабування та посилення тіні
- **Active стан** з анімацією натискання
- **Адаптивні розміри** для всіх пристроїв
- **Високий z-index** для гарантованої видимості

### Адаптивність:
- Desktop (>768px): повна таблиця з 4 колонками, 3 колонки товарів
- Mobile (<768px): карточковий вигляд кошика, 1 колонка товарів
- Tablet: проміжні стилі для оптимального відображення

---

## [2026-04-30]

### Нова функціональність
- Додано поле `color` (ForeignKey → Color, nullable) у модель `ProductImage` — дозволяє прив'язувати фото галереї до конкретного кольору товару
- Створено міграцію `0010_productimage_color.py`
- Оновлено `ProductImageInline` у `admin.py` — поле `color` тепер доступне при завантаженні фото в адмін-панелі
- Оновлено `product_detail` view у `views.py` — будується словник `color_images` (`{colorName: [urls], "__all__": [urls]}`) і передається у шаблон як JSON
- Оновлено JS у `product-detail.html`:
  - При виборі кольору викликається `updateGalleryForColor(colorName)` — головне фото і мініатюри автоматично замінюються фото обраного кольору
  - Фото без прив'язки до кольору (`__all__`) показуються для всіх варіантів
  - Функція `renderThumbnails()` динамічно перебудовує стрічку мініатюр
  - При автоматичному виборі першого доступного варіанту також оновлюється галерея

### CSS / UI
- Повний рефакторинг сторінки деталі товару (`product-detail.html` + `style.css`)
- Додано нову секцію `/* ---------- PRODUCT DETAIL ---------- */` у `style.css`
- Галерея: головне фото збільшено до 480px висоти з `object-fit: contain`, плавний CSS-перехід `opacity` при зміні фото
- Мініатюри: фіксований розмір 80×80px, `object-fit: cover`, рожева рамка при активному стані + hover-ефект
- Стрілки галереї: стилізовані круглі кнопки з `fas fa-chevron-*`, hover змінює фон на `var(--pink)`
- Картка товару: білий контейнер `.pd-card` з тінню `box-shadow`, `border-radius: 16px`
- Додано `pd-category-tag` — рожевий пілюля-тег категорії над назвою
- Зірковий рейтинг: жовтий колір `#f7c948` для заповнених зірок, пустий `#e0c0d0`
- Характеристики: блок `.pd-specs` на світлому рожевому фоні, `<dl>` із рядками `.pd-spec-row` (ключ ліворуч, значення праворуч), `.pd-specs-raw` приховано
- Розміри: pill-кнопки `.pd-size-btn` з `border: 2px solid`, виразний `.selected`-стан
- Кольори: круглі `.pd-color-dot` 36×36px, рожева рамка + `scale(1.12)` при виборі
- Кількість: `.pd-qty-control` — стилізований інпут з кнопками `+/−`, прибрано стрілки браузера
- Кнопка «Додати до кошика»: повна ширина, градієнт, іконка кошика, ефект підйому при hover
- Секція відгуків: `.pd-reviews` — білий блок з тінню, кожен відгук відокремлено лінією `#fce4ec`
- Схожі товари: grid-сітка `.pd-related-grid` з hover-ефектом
- Адаптивність: медіазапити для `≤900px` (вертикальне розташування) та `≤576px` (мобільний)

---

## [2026-04-28]

### Налаштування середовища
- Надано права на запис для всіх файлів проекту (`chmod -R u+w`)
- Виправлено права на папки `.venv/` та `node_modules/` (були `dr-xr-xr-x`)

### Залежності
- Встановлено `psycopg2-binary==2.9.12` у venv (не було в requirements.txt)
- Встановлено `django-redis==6.0.0` та `redis==7.4.0`
- Виправлено `requirements.txt` — прибрано null bytes (BOM), додано нові пакети

### Конфігурація
- Створено `.gitignore` — ігнорує `venv/`, `.venv/`, `node_modules/`, `db.sqlite3`, `staticfiles/`, `media/`, `.env`, `__pycache__/`, `.idea/` тощо
- Створено `Makefile` з командами для Docker, Django, CSS та очистки

### Docker
- Наявний `docker-compose.yml` з сервісами `postgres:16` та `redis:7-alpine`

### CSS / UI
- Оновлено `shop/static/main/style.css`: рефакторинг стилів шапки, сайдбару, карток товарів, пагінації, відгуків, адаптивності

### Документація
- Створено папку `docs/` та цей файл `CHANGELOG.md`

### Виправлення (Bug Fix)
- **Бургер-меню**: sidebar перенесено у `base.html` через `{% block sidebar %}` — тепер кнопка бургер прихована на сторінках без сайдбару (JS ховає кнопку якщо `sidebar` порожній), на каталозі працює коректно
- Вміст сайдбару (фільтри) виокремлено в `{% block sidebar %}` у `product-list.html`
- Створено `main/context_processors.py` — передає `all_categories` у всі шаблони
- Зареєстровано `main.context_processors.categories` у `shop/settings.py`
- `base.html`: sidebar тепер має вміст за замовчуванням — список категорій (навігація по каталогу на всіх сторінках)
- **Toast при додаванні в кошик**: `cart/views.py` — редіректить назад (`HTTP_REFERER`) замість на сторінку кошика; `base.html` — toast-блок з Django messages; `style.css` — стилі `.toast` з анімацією появи/зникнення (автозакриття через 3 сек)

---

## [2026-04-29]

### Виправлення (Bug Fix)
- **Docker orphan-контейнери**: `make clean-docker` доповнено прапором `--remove-orphans` — тепер `shopdj_redis` / `shopdj_postgres` коректно видаляються при очистці

### CSS / UI
- **Empty state на каталозі**: замість рядка «Немає товарів 😢» відображається стилізований блок з іконкою 🛍️, заголовком, підказкою та кнопкою «Показати всі товари» (`.empty-state`, `style.css`)
- **Пагінація**: прихована якщо сторінок ≤ 1 (`{% if page_obj.paginator.num_pages > 1 %}`)
- **Sticky footer**: `body` — `min-height: 100vh; display: flex; flex-direction: column`; `base.html` — `<div class="page-wrapper">` обгортає `<main>` і `<footer>`; `.page-wrapper` та `.page-wrapper main` отримали `flex: 1` — футер завжди притиснутий до низу незалежно від обсягу контенту
- **Empty state кошика**: `cart/templates/cart/detail.html` — замінено рядок «Ваш кошик порожній» на стилізований блок з іконкою 🛒, заголовком, підказкою та кнопкою «Перейти до каталогу» (використовує існуючі стилі `.empty-state`)

### Виправлення (Bug Fix)
- **Невидима кнопка «Увійти»**: `auth.css` використовував невизначену змінну `var(--primary-color)` — замінено на `var(--pink)`; також `var(--dark-gray)` → `var(--dark-grey)`, `var(--secondary-color)` → `var(--pink)`
- **Стилізація інформаційного рядка шапки**: додано стилі `.user-info-header` (рожева тонка лінія зверху, дрібний текст, емодзі 👋) та `.product-count` (рожевий градієнтний badge) у `style.css`
- **Стилізація бічної панелі**: градієнтний фон, рожева рамка, заокруглені кути; `.sidebar-header` — рожевий gradient-заголовок «ФІЛЬТРИ» з кнопкою закриття ✕; `.dropdown-toggle` — gradient, `.dropdown-content a` — точки-маркери, рожева ліва межа при hover/active

## [2026-04-29] — Кастомна адмін-панель

### CSS / UI
- **Рожева тема Django admin**: `shop/static/admin/css/custom.css` — перекриває стандартні стилі (заголовок, кнопки, фільтри, таблиці, форми, пагінація)
- **`shop/templates/admin/base_site.html`** — кастомний брендинг «👗 LadiesDream», красива шапка з кнопками-пігулками для користувача
- **Примусова світла тема**: `{% block dark-mode-vars %}{% endblock %}` + порожній `color_theme_toggle.html` — повністю відключає темну тему і перемикач
- **Прихована бічна панель**: `#nav-sidebar { display: none }` — замінена розділом «Управління» на дашборді
- **Горизонтальне навігаційне меню** під шапкою: Дашборд, Товари, Категорії, Замовлення, Користувачі, Відгуки, Промокоди — з підсвіткою активного пункту

### Нова функціональність
- **`admin_site.py`** (корінь проекту) — клас `LadiesDreamAdminSite` зі статистикою: товари, замовлення, виручка, користувачі, відгуки, промокоди, останні замовлення
- **`shop/templates/admin/index.html`** — дашборд з картками статистики, таблицею останніх замовлень та сіткою розділів «Управління» (`app_list`)
- **`shop/templates/admin/color_theme_toggle.html`** — порожній файл для відключення перемикача теми
- **Всі `admin.py`** (`main`, `orders`, `accounts`, `discounts`, `reviews`) — `@decorator` замінено на `admin_site.register()` викликами; реєстрація через `AppConfig.ready()`
- **`shop/urls.py`** — використовує `admin_site.urls`
- **`shop/settings.py`** — `SimpleAdminConfig` замість `django.contrib.admin`, `reviews`/`discounts` вказані з `AppConfig`, `shop/templates` в `TEMPLATES.DIRS`

### Виправлення (Bug Fix)
- **`TypeError: 'NoneType' object is not callable`**: `@admin_site.register()` не є декоратором в `AdminSite` — замінено на явні виклики `admin_site.register(Model, AdminClass)`
- **Circular import**: `django.contrib.admin.apps.SimpleAdminConfig` вимикає `autodiscover`; реєстрація через `AppConfig.ready()`

## [2026-04-29] — Модель Supplier

### Нова функціональність
- **`main/models.py`** — нова модель `Supplier` (назва, сайт, нотатки); `Product.supplier` перетворено з `CharField(choices=...)` на `ForeignKey(Supplier)`
- **`main/admin.py`** — `SupplierAdmin` з підрахунком товарів; `supplier` додано до `list_display` і `list_filter` в `ProductAdmin`
- **`main/views.py`** — `SUPPLIER_CHOICES` замінено на `Supplier.objects.all()` в контексті
- **Навігаційне меню адмінки** — додано пункт «🏭 Постачальники»

### Міграції
- **`main/0009_supplier_alter_product_supplier.py`** — створено таблицю `Supplier`, змінено поле `supplier` на FK

## [2026-04-29] — Покращення адмін-панелі

### Нова функціональність
- **`orders/models.py`** — нове поле `status` (new/processing/shipped/delivered/cancelled) + `STATUS_CHOICES`
- **`orders/admin.py`** — кольорові бейджі `status_badge`, `paid_badge`, `delivery_badge`; пошук по імені/email/телефону/місту; фільтри по статусу/оплаті/доставці; 6 batch-дій (зміна статусу, позначення оплати); покращений CSV-експорт
- **`main/admin.py`** — превью зображення товару (`image_preview`) в списку; `autocomplete_fields = ('supplier',)` в `ProductAdmin`
- **`admin_site.py`** — нові показники: середній чек, продажі по днях (7 днів), топ-5 товарів, кількість нових замовлень
- **`shop/templates/admin/index.html`** — блок «Швидкі дії», CSS bar-chart продажів за 7 днів, топ-5 товарів, колонка статусу в таблиці замовлень

### Міграції
- **`orders/0009_order_status.py`** — додано поле `status` до `Order`

## [2026-04-29] — Детальна сторінка замовлення в адмінці

### CSS / UI
- **`shop/templates/admin/orders/order/change_form.html`** — кастомний шаблон сторінки замовлення:
  - Заголовок з номером і датою замовлення
  - Картка клієнта (ім'я, email, телефон, посилання на акаунт)
  - Картка доставки і статусу (місто, адреса, тип доставки, бейджі оплати та статусу, промокод)
  - Блок із загальною сумою і знижкою
  - Таблиця позицій замовлення з посиланнями на товари і підсумком

## [2026-04-29] — Виправлення і стилізація UI

### Виправлення (Bug Fix)
- **`shop/static/admin/css/custom.css`** — виправлено невидимий текст на кнопках «Шукати» і «Виконати» в адмінці: додані явні селектори `#changelist-search input[type=submit]` і `.actions button[type=submit]` з `color: #fff !important`

### CSS / UI
- **`main/admin.py`** — додано `prepopulated_fields = {"slug": ("name",)}` в `ProductAdmin` — поле URL-адреса автоматично заповнюється з назви товару
- **`orders/templates/orders/checkout.html`** — кнопка «На головну» стилізована як рожева пігулка `.btn-home` на всю ширину
- **`shop/static/main/cart.css`** — повна переробка стилів кошика:
  - Заголовок таблиці: рожевий градієнт
  - Рядки товарів: рожева межа, hover-підсвітка
  - Зображення: заокруглені кути, рожева рамка
  - Форма кількості: `display: flex` — поле і кнопка «Оновити» в одному рядку
  - Кнопка «Оновити»: рожева пігулка
  - Кнопка «Видалити»: кругла, рожева рамка → заповнюється при hover
  - Блок підсумку: карточка з градієнтом, контурна «Продовжити» + рожева «Оформити»
  - Порожній кошик: стилізована заглушка
- **`cart/templates/cart/detail.html`** — замість відсутнього зображення товару показується placeholder 👗 у рожевому квадраті (`.cart-item-no-image`)
